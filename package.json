{
  "name": "clean-metadata-tool-root",
  "private": true,
  "version": "1.1.0",
  "scripts": {
    "postinstall": "echo \"Tip: run 'npm --prefix client install' and 'npm --prefix server install'\"",
    "dev": "concurrently \"npm --prefix client run dev\" \"npm --prefix server run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
